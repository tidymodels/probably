# S3 methods to track which additional packages are needed for prediction intervals via conformal inference

S3 methods to track which additional packages are needed for prediction
intervals via conformal inference

## Usage

``` r
# S3 method for class 'int_conformal_cv'
required_pkgs(x, infra = TRUE, ...)

# S3 method for class 'int_conformal_full'
required_pkgs(x, infra = TRUE, ...)

# S3 method for class 'int_conformal_quantile'
required_pkgs(x, infra = TRUE, ...)

# S3 method for class 'int_conformal_split'
required_pkgs(x, infra = TRUE, ...)
```

## Arguments

- x:

  a conformal interval object

- ...:

  Other arguments passed to methods
