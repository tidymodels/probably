# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r:
  - devel
  - release
  - oldrel
  - 3.2
  - 3.1

matrix:
  allow_failures:
  - r: 3.1
